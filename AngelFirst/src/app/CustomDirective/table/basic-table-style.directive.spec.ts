import { BasicTableStyleDirective } from './basic-table-style.directive';

describe('BasicTableStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicTableStyleDirective();
    expect(directive).toBeTruthy();
  });
});
